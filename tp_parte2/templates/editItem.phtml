<?php include 'header.phtml'; ?>

<h1 class="my-4">Editar Película</h1>

<form action="updateItem/<?= $item->id_pelicula ?>" method="POST" class="p-3 border rounded">
    <div class="mb-3">
        <label for="nombre" class="form-label">Nombre</label>
        <input type="text" class="form-control" id="nombre" name="nombre" value="<?= htmlspecialchars($item->nombre) ?>" required>
    </div>
    <div class="mb-3">
        <label for="estudio" class="form-label">Estudio</label>
        <input type="text" class="form-control" id="estudio" name="estudio" value="<?= htmlspecialchars($item->estudio) ?>" required>
    </div>
    <div class="mb-3">
        <label for="id_genero" class="form-label">Categoría</label>
        <select class="form-select" id="id_genero" name="id_genero" required>
            <?php foreach ($categories as $category): ?>
                <option value="<?= $category->id ?>" <?= ($category->id == $item->id_genero) ? 'selected' : '' ?>>
                    <?= htmlspecialchars($category->nombre) ?>
                </option>
            <?php endforeach; ?>
        </select>
    </div>
    
    <button type="submit" class="btn btn-primary">Guardar Cambios</button>
    <a href="adminItems" class="btn btn-secondary">Cancelar</a>
</form>

<?php include 'footer.phtml'; ?>